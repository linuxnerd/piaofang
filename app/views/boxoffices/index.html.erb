<div class='ui grid'>
<div class='ui two wide column'></div>
<div class='ui twelve wide column'>

<div class="ui pointing secondary boxoffice menu">
  <a class="red active item" data-tab="US">北美</a>
  <a class="orange item" data-tab="CN">内地</a>
  <a class="green item" data-tab="HK">香港</a>
  <a class="blue item" data-tab="TW">台湾</a>
  <a class="teal item" data-tab="JP">日本</a>
  <a class="purple item" data-tab="KR">韩国</a>
  <a class="red item" data-tab="FR">法国</a>
  
</div>

<div class="ui active tab segment" data-tab="US">
  <div class="ui animated list ">
    <% @us.each do |item| %>
      <div class="ui <%= item.rid<=3 ? 'red' : 'teal' %> ribbon label">
        第<%= item.rid %>名
      </div>
      <div id='<%= item.area %><%=item.rid %>' class="item" >
        <img alt="<%= item.name %>" src="<%= item.image_url %>", class = 'ui circular small image' />
        <div class="content">
          <h3 class="ui header">
            <%= item.name %>
            <%= render_rating(item.rating) %>
          </h3>
          <i class="grid layout circular icon link"></i>
        </div>
      </div>
      <div class="ui divider"></div>
      
      <div id='<%= item.area %><%=item.rid %>_modal' class="ui modal">
        <i id='<%= item.area %><%=item.rid %>_icon' class="close icon"></i>
        <div class="header">
            <%= item.name %>
        </div>
        <div class="content">
          <div class="ui header">你确定要创建一个视频教程专辑吗？</div>
          <p>如果你确定要创建视频专辑专辑请单击确定。</p>
          <p>如果你觉得暂时还不需要创建视频教程专辑，请点击否。</p>
        </div>
      </div>

      <script type="text/javascript">
        $('#<%= item.area %><%=item.rid %>').popup({
          position : 'left center',
          title    : '<%= item.release_date %>上映',
          content  : '主演：<%= item.actors %>'
        });

        $('#<%= item.area %><%=item.rid %>_modal')
          .modal('attach events', '#<%= item.area %><%=item.rid %>_icon', 'show');
      </script>

    <% end %>
  </div>
</div>

<div class="ui tab " data-tab="CN">
  <%= render_table(@cn,
          [
            { :name => 'rid', :display_name => '排名' },
            { :name => 'name', :display_name => '影片名称' },
            { :name => 'wk', :display_name => '榜单周数' },
            { :name => 'wboxoffice', :display_name => '周末票房' },
            { :name => 'tboxoffice', :display_name => '累计票房'}
          ]) %>
</div>
<div class="ui tab " data-tab="HK">
</div>
<div class="ui tab " data-tab="TW">
</div>
<div class="ui tab " data-tab="JP">
</div>
<div class="ui tab " data-tab="KR">
</div>
<div class="ui tab " data-tab="FR">
</div>

</div>


</div>